import { Basic } from '../locale';

const code = `
import { Alert } from '@mx-design/web';

function App() {
  return <Alert content='Here is an example text' />;
}`;

export const basic = {
  code,
  namespace: Basic,
};
