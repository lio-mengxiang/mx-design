import { basic } from './basic';
import { customIcon } from './customIcon';
import { customPic } from './customPic';

export const exampleList = {
  [basic.namespace]: basic,
  [customIcon.namespace]: customIcon,
  [customPic.namespace]: customPic,
};
