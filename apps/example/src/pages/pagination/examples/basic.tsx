import { Basic } from '../locale';

const code = `
import { Pagination } from '@mx-design/web';

function App() {
  return <Pagination total={90} />;
};`;

export const basic = {
  code,
  namespace: Basic,
};
