import { Basic } from '../locale';

const code = `
import { Space, Switch } from '@mx-design/web';

function App() {
  return (
    <Space>
      <Switch />
    </Space>
  );
}`;

export const basic = {
  code,
  namespace: Basic,
};
